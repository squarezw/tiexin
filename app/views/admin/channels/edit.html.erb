<% page_title _('Modify Channel')
   side_menu 'master_data' -%>

<%= error_messages_for :channel %>

<% form_for :channel, @channel, :url=>admin_channel_path(@channel), :html=>{:method=>:put, :multipart=>true} do |f| -%>
	<%= render :partial => "form", :object=>f %>
<% end -%>